CREATE TABLE IF NOT EXISTS public.meal_logs (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, user_id uuid NOT NULL, meal_data jsonb, photo_url text, logged_at timestamptz DEFAULT now() NOT NULL, created_at timestamptz DEFAULT now() NOT NULL);
CREATE TABLE IF NOT EXISTS public.metric_goals (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, user_id uuid NOT NULL, metric_name text NOT NULL, metric_key text, target_value real, current_value real, created_at timestamptz DEFAULT now() NOT NULL);
CREATE TABLE IF NOT EXISTS public.monthly_assessments (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, user_id uuid NOT NULL, assessed_at timestamptz DEFAULT now() NOT NULL, peso real, weight real, altura real, body_fat real, gordura real, braco real, cintura real, coxa real, quadril real, peitoral real, panturrilha real, measurements jsonb, photos jsonb, foto_frente text, foto_costas text, foto_lateral text, foto_lado_direito text, foto_lado_esquerdo text, notes text, created_at timestamptz DEFAULT now() NOT NULL);
CREATE TABLE IF NOT EXISTS public.notifications (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, user_id uuid NOT NULL, title text NOT NULL, body text, type text, metadata jsonb, read boolean NOT NULL DEFAULT false, created_at timestamptz DEFAULT now() NOT NULL);
CREATE TABLE IF NOT EXISTS public.payments (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, user_id uuid NOT NULL, amount numeric NOT NULL DEFAULT 0, status text NOT NULL DEFAULT 'pending', gateway_transaction_id text, subscription_plan_id uuid REFERENCES public.subscription_plans(id), created_at timestamptz DEFAULT now() NOT NULL);
CREATE TABLE IF NOT EXISTS public.pending_checkouts (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, email text NOT NULL, nome text NOT NULL, password text NOT NULL, telefone text, plan_id text, checkout_reference text NOT NULL DEFAULT gen_random_uuid()::text, created_at timestamptz DEFAULT now() NOT NULL);
CREATE TABLE IF NOT EXISTS public.premiacoes (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, categoria text NOT NULL, mes_referencia text NOT NULL, descricao_kit text, foto_kit_url text, "campeã_user_id" uuid REFERENCES public.profiles(id), sorteio_realizado boolean DEFAULT false, created_at timestamptz DEFAULT now());
CREATE TABLE IF NOT EXISTS public.presence (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, user_id uuid NOT NULL, online boolean NOT NULL DEFAULT false, last_seen timestamptz, updated_at timestamptz DEFAULT now() NOT NULL);
CREATE TABLE IF NOT EXISTS public.psych_checkins (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, user_id uuid NOT NULL, mood integer, stress integer, sleep_quality integer, sleep_hours real, notes text, created_at timestamptz DEFAULT now() NOT NULL);
CREATE TABLE IF NOT EXISTS public.push_subscriptions (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, user_id uuid NOT NULL, endpoint text NOT NULL, keys jsonb NOT NULL, p256dh text, auth text, created_at timestamptz DEFAULT now() NOT NULL);
CREATE TABLE IF NOT EXISTS public.sorteio_log (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, premiacao_id uuid REFERENCES public.premiacoes(id), vencedora_user_id uuid REFERENCES public.profiles(id), executado_por uuid REFERENCES public.profiles(id), executado_em timestamptz DEFAULT now(), candidatas jsonb);
CREATE TABLE IF NOT EXISTS public.streaks (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, user_id uuid NOT NULL, current_streak integer NOT NULL DEFAULT 0, best_streak integer NOT NULL DEFAULT 0, last_checkin_date date, updated_at timestamptz DEFAULT now() NOT NULL);
CREATE TABLE IF NOT EXISTS public.student_specialists (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, student_id uuid NOT NULL, specialist_id uuid NOT NULL, specialty text NOT NULL DEFAULT 'personal', created_at timestamptz DEFAULT now() NOT NULL);
CREATE TABLE IF NOT EXISTS public.subscriptions (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, user_id uuid NOT NULL, subscription_plan_id uuid REFERENCES public.subscription_plans(id), status text NOT NULL DEFAULT 'active', payment_status text, plan_price numeric NOT NULL DEFAULT 0, started_at timestamptz DEFAULT now() NOT NULL, canceled_at timestamptz, cancel_reason text, created_at timestamptz DEFAULT now() NOT NULL);
CREATE TABLE IF NOT EXISTS public.training_plans (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, user_id uuid NOT NULL, name text NOT NULL DEFAULT '', title text, status text NOT NULL DEFAULT 'draft', active boolean DEFAULT true, groups jsonb, plan_data jsonb, objetivo_mesociclo text, specialist_id uuid, valid_until timestamptz, version integer NOT NULL DEFAULT 1, created_at timestamptz DEFAULT now() NOT NULL, updated_at timestamptz DEFAULT now() NOT NULL);
CREATE TABLE IF NOT EXISTS public.training_plan_versions (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, plan_id uuid NOT NULL REFERENCES public.training_plans(id) ON DELETE CASCADE, version_number integer NOT NULL, title text, plan_data jsonb, saved_at timestamptz DEFAULT now() NOT NULL, created_at timestamptz DEFAULT now() NOT NULL);
CREATE TABLE IF NOT EXISTS public.training_templates (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, name text NOT NULL, description text, specialist_id uuid NOT NULL, template_data jsonb, created_at timestamptz DEFAULT now() NOT NULL);
CREATE TABLE IF NOT EXISTS public.user_notifications_read (user_id uuid NOT NULL REFERENCES public.profiles(id) ON DELETE CASCADE, notification_id uuid NOT NULL REFERENCES public.broadcast_notifications(id) ON DELETE CASCADE, read_at timestamptz DEFAULT now(), PRIMARY KEY (user_id, notification_id));
CREATE TABLE IF NOT EXISTS public.user_photos (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, user_id uuid NOT NULL, photo_url text NOT NULL, category text, notes text, created_at timestamptz DEFAULT now() NOT NULL);
CREATE TABLE IF NOT EXISTS public.user_selected_plans (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, user_id uuid NOT NULL, plan_type text NOT NULL, challenge_id uuid, module_id uuid, source_plan_id uuid, plan_data jsonb, created_at timestamptz DEFAULT now() NOT NULL);
CREATE TABLE IF NOT EXISTS public.user_shopping_list_items (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, user_id uuid NOT NULL, item_name text NOT NULL, checked boolean DEFAULT false, updated_at timestamptz DEFAULT now() NOT NULL);
CREATE TABLE IF NOT EXISTS public.volume_limits (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, specialist_id uuid NOT NULL, student_id uuid, muscle_group text NOT NULL, min_sets integer, max_sets integer, created_at timestamptz DEFAULT now() NOT NULL);
CREATE TABLE IF NOT EXISTS public.webhook_logs (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, event_type text, email text, status_log text NOT NULL DEFAULT 'received', raw_payload jsonb, created_at timestamptz DEFAULT now() NOT NULL);
CREATE TABLE IF NOT EXISTS public.workout_logs (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, user_id uuid NOT NULL, training_plan_id uuid REFERENCES public.training_plans(id), workout_data jsonb, duration_minutes integer, completed_at timestamptz DEFAULT now() NOT NULL, created_at timestamptz DEFAULT now() NOT NULL);
CREATE TABLE IF NOT EXISTS public.workouts (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, user_id uuid NOT NULL, training_plan_id uuid REFERENCES public.training_plans(id), group_name text, exercises jsonb, workout_data jsonb, comment text, effort_rating integer, duration_minutes integer, duration_seconds integer, started_at timestamptz, finished_at timestamptz, created_at timestamptz DEFAULT now() NOT NULL);

INSERT INTO storage.buckets (id,name,public) VALUES ('community_media','community_media',true) ON CONFLICT (id) DO NOTHING;
INSERT INTO storage.buckets (id,name,public) VALUES ('avatars','avatars',true) ON CONFLICT (id) DO NOTHING;
